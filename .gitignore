/target
# Cargo.lock is committed (per current Cargo guidance, even for libraries) so
# CI tests a reviewed, pinned dependency set. It does not affect downstream
# consumers — they resolve their own lockfile.
**/*.rs.bk
*.pdb
.DS_Store
*.swp
*.swo
.vscode/
.idea/
clippy_out.txt
AUDIT_REPORT.md
